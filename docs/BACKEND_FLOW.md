# Backend flow

The full **interface → cache → simulate** narrative, API tables, and file references are maintained in the **[main README](../README.md)** (sections *API flow* and *Repository layout*).

This file is kept as a short pointer so older links still resolve.
